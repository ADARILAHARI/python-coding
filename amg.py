def armstrong(num):
    num = str(number)
    num_digits = len(num)
    for num_digit in num:
        sum += int(digit)** num_digits

        if sum == number:
            return true
        else:
            return false

num = int(input())

    
